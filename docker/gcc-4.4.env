#!/bin/bash

echo 1 | update-alternatives --config gcc

source /root/etc/versions.env

export build_dir=/root/build/gcc-6
export install_dir=/root/opt/gcc-6
export CC=`which gcc`
export CXX=`which g++`
export FC=`which gfortran`
